---
description: Show Repo Lens roadmap status (done / in-progress / next) — no changes
---

Read `docs/ROADMAP.md` and print a concise status summary. Do NOT modify anything or write code.

- Count units by status per goal (e.g. "Goal 2 — Ask cleanup: 1/3 done").
- List completed `[x]` units (just IDs + titles).
- Show the single **next** unit `/next` would pick (first `[ ]`, skipping `[!]`), with its acceptance criteria.
- List any `[!]` blocked units and their blocker note.
- Keep it short — a status board, not the full file.
